package main

func main() {
	// init main

	// init connection
	/*
		proto := "tcp"
		ip_addr := "127.0.0.1"
		port := "9999"
		target := ip_addr + ":" + port
		conn, err := net.Dial(proto, addr)
		if (err!=nil) {}
	*/

	// receive command
	// analysis command keyword
	// client process(local) / server process(socket)
}
